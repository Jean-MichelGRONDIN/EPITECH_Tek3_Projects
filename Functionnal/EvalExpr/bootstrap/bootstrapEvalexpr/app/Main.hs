module Main where

import Lib

main :: IO ()
main = putStr ""


-- un parse tuple custom qui appelle un aprseTuple quand le truc qui veut recup c'est un tuple
-- (utilisé parseOr pour fair si c'est ma vulue or un new tuple)